package user

import "github.com/kalpio/allsell/src/views/layout"

import (
    "github.com/kalpio/allsell/src/types/user"
    "github.com/kalpio/allsell/src/views/layout"
    )

templ List(username string, users []user.User) {
   @layout.Layout(username) {

   }
}